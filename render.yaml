services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      mkdir -p data
      cp processes.json data/ || true
    startCommand: python bot.py